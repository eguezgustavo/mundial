[project]
name = "mundial-sync"
version = "0.1.0"
description = "FIFA World Cup 2026 data sync for the Mundial prediction app"
requires-python = ">=3.11"
dependencies = [
    "firebase-admin==6.5.0",
    "requests==2.31.0",
    "python-dotenv==1.0.1",
    "python-dateutil==2.9.0",
]
